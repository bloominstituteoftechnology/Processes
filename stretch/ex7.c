// Write an additional program that creates two processes P1 and P2.
// P1 should accept a string from standard input and passes it to
// P2. P2 should concatenate the received string with another string
// and sends it back to P1 for printing.

#include<stdio.h>
#include<stdlib.h>
#include<unistd.h>
#include<sys/types.h>
#include<string.h>
#include<sys/wait.h>

int main(int argc, char const *argv[])
{
  /* code */
  return 0;
}